import { colors } from "./colors";
import { space, spacingLayout, lineHeights } from "./spacing";
import { sizes, deviceSize } from "./sizes";
import {
  fontConfig,
  fontSizes,
} from "./fonts";

export const theme = {
  colors,
  space,
  spacingLayout,
  lineHeights,
  sizes,
  deviceSize,
  fontConfig,
  fontSizes,
};