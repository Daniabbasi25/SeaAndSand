import React from 'react';
import { View, StyleSheet, Image } from 'react-native';
import { TouchableScale, TextApp } from '@BaseComponent';

const ThirdScreen = ({}) => {
    return (
        <View>
            
        </View>
    )
}
const styles = StyleSheet.create({

});

export default React.memo(ThirdScreen);